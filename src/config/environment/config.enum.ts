export enum PAYMENT_PROVIDER_ENUM {
  local = "local",
  stripe = "stripe",
}

export enum BANK_PROVIDER_ENUM {
  local = "local",
  brasilApi = "brasilApi",
}

export enum GEOLOCATION_PROVIDER_ENUM {
  local = "local",
  google = "google",
}

export enum ADDRESS_PROVIDER_ENUM {
  local = "local",
  brasilApi = "brasilApi",
}
