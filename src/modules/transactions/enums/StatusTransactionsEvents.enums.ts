export enum STATUS_TRANSACTION_ENUM {
  PROGRESS = "progress",
  FINISHED = "finished",
  CANCELED = "canceled",
  REFUNDS = "refunds",
}

export enum STATUS_EVENTS_TRANSACTIONS_ENUM {
  PAYMENT = "payment",
  REFUND = "refund",
  PARTIAL = "partial",
}
